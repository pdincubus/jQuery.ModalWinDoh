/**
* ModalWinDoh plugin
*
* @author Phil Steer
* https://github.com/pdincubus/jquery.DarthPhader
*/

(function($) {
    $.fn.modalWinDoh = function(options) {

        // settings
        var settings = $.extend({

        }, options);

        return this.each(function() {

        });
    };
})(jQuery);
